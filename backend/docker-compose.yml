version: '3.8'

services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: nutriguard
      POSTGRES_PASSWORD: nutriguard_password
      POSTGRES_DB: nutriguard_test
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U nutriguard -d nutriguard_test"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
